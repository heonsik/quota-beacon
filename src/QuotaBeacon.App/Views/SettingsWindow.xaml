<Window x:Class="QuotaBeacon.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:theming="clr-namespace:QuotaBeacon.App.Theming"
        Title="{Binding [Settings.Title], Source={x:Static theming:Localization.Current}}"
        Width="470"
        Icon="pack://application:,,,/Assets/QuotaBeacon.ico"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Surface}"
        Foreground="{DynamicResource Brush.Foreground}"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Ideal"
        FontFamily="Segoe UI Variable Text, Segoe UI">

  <!-- The default WPF control chrome is a decade older than the card it configures. These templates
       restate the card's vocabulary — 6px radii, hairline borders, accent for the active state — so
       the two surfaces read as one product. -->
  <Window.Resources>
    <Style TargetType="TextBlock">
      <Setter Property="VerticalAlignment" Value="Center" />
      <Setter Property="FontSize" Value="12.5" />
    </Style>

    <Style x:Key="SectionHeading" TargetType="TextBlock">
      <Setter Property="FontSize" Value="11.5" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Foreground" Value="{DynamicResource Brush.Muted}" />
      <Setter Property="Margin" Value="2,0,0,8" />
    </Style>

    <!-- Grouping the rows into soft panels gives the dialog the same layered feel as the card, and
         makes the two concerns readable at a glance instead of as one long list. -->
    <Style x:Key="Group" TargetType="Border">
      <Setter Property="Background" Value="{DynamicResource Brush.Track}" />
      <Setter Property="BorderBrush" Value="{DynamicResource Brush.CardBorder}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="10" />
      <Setter Property="Padding" Value="16,14" />
    </Style>

    <Style TargetType="TextBox">
      <Setter Property="Width" Value="104" />
      <Setter Property="Height" Value="34" />
      <Setter Property="FontSize" Value="12.5" />
      <Setter Property="Padding" Value="10,0" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="Foreground" Value="{DynamicResource Brush.Foreground}" />
      <Setter Property="CaretBrush" Value="{DynamicResource Brush.Foreground}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TextBox">
            <Border x:Name="Root"
                    Background="{DynamicResource Brush.Elevated}"
                    BorderBrush="{DynamicResource Brush.CardBorder}"
                    BorderThickness="1"
                    CornerRadius="6">
              <ScrollViewer x:Name="PART_ContentHost"
                            Padding="{TemplateBinding Padding}"
                            VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource Brush.Text.Normal}" />
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush.Hover}" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="Button">
      <Setter Property="FontSize" Value="12.5" />
      <Setter Property="Padding" Value="14,0" />
      <Setter Property="Height" Value="34" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Foreground" Value="{DynamicResource Brush.Foreground}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Root"
                    Background="{DynamicResource Brush.Elevated}"
                    BorderBrush="{DynamicResource Brush.CardBorder}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush.Hover}" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Root" Property="Opacity" Value="0.75" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- Save carries the accent because it is the one action that commits. -->
    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
      <Setter Property="Foreground" Value="White" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Root"
                    Background="{DynamicResource Brush.Fill.Normal}"
                    CornerRadius="6"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Root" Property="Opacity" Value="0.9" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Root" Property="Opacity" Value="0.75" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="CheckBox">
      <Setter Property="FontSize" Value="12.5" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Foreground" Value="{DynamicResource Brush.Foreground}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <StackPanel Orientation="Horizontal" Background="Transparent">
              <Border x:Name="Box"
                      Width="18" Height="18"
                      CornerRadius="5"
                      Background="{DynamicResource Brush.Elevated}"
                      BorderBrush="{DynamicResource Brush.CardBorder}"
                      BorderThickness="1"
                      VerticalAlignment="Center">
                <Path x:Name="Tick"
                      Width="10" Height="10"
                      Stretch="Uniform"
                      Visibility="Collapsed"
                      Stroke="White"
                      StrokeThickness="2"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"
                      StrokeLineJoin="Round"
                      Data="M1 5.5 L4 8.5 L9 1.5" />
              </Border>
              <ContentPresenter Margin="9,0,0,0" VerticalAlignment="Center" />
            </StackPanel>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Box" Property="Background" Value="{DynamicResource Brush.Fill.Normal}" />
                <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Brush.Text.Normal}" />
                <Setter TargetName="Tick" Property="Visibility" Value="Visible" />
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Brush.Text.Normal}" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
      <Setter Property="Padding" Value="10,7" />
      <Setter Property="FontSize" Value="12.5" />
      <Setter Property="Foreground" Value="{DynamicResource Brush.Foreground}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBoxItem">
            <Border x:Name="Root" Background="Transparent" CornerRadius="4" Padding="{TemplateBinding Padding}">
              <ContentPresenter />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush.Hover}" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="ComboBox">
      <Setter Property="FontSize" Value="12.5" />
      <Setter Property="Height" Value="34" />
      <Setter Property="Foreground" Value="{DynamicResource Brush.Foreground}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBox">
            <Grid>
              <ToggleButton x:Name="Toggle"
                            Focusable="False"
                            ClickMode="Press"
                            Cursor="Hand"
                            IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                <ToggleButton.Template>
                  <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Root"
                            Background="{DynamicResource Brush.Elevated}"
                            BorderBrush="{DynamicResource Brush.CardBorder}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="10,0">
                      <Grid>
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
                        <Path HorizontalAlignment="Right" VerticalAlignment="Center"
                              Width="9" Height="6" Stretch="Uniform"
                              Fill="{DynamicResource Brush.Muted}"
                              Data="M0 0 L5 5 L10 0 Z" />
                      </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                      <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="Root" Property="Background" Value="{DynamicResource Brush.Hover}" />
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </ToggleButton.Template>
              </ToggleButton>

              <ContentPresenter Margin="10,0,26,0"
                                VerticalAlignment="Center"
                                IsHitTestVisible="False"
                                Content="{TemplateBinding SelectionBoxItem}" />

              <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                     Placement="Bottom"
                     AllowsTransparency="True"
                     PopupAnimation="Fade"
                     Focusable="False">
                <Border Background="{DynamicResource Brush.Elevated}"
                        BorderBrush="{DynamicResource Brush.CardBorder}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Margin="0,4,0,0"
                        Padding="4"
                        MinWidth="{TemplateBinding ActualWidth}">
                  <StackPanel IsItemsHost="True" />
                </Border>
              </Popup>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Grid Margin="24,22,24,20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel>
      <TextBlock Style="{StaticResource SectionHeading}"
                 Text="{Binding [Settings.Providers], Source={x:Static theming:Localization.Current}}" />

      <Border Style="{StaticResource Group}">
      <StackPanel>
      <CheckBox x:Name="ClaudeEnabled" Content="Claude" />
      <Grid Margin="27,7,0,12">
        <TextBlock x:Name="ClaudeSession" Foreground="{DynamicResource Brush.Muted}" FontSize="11.5" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <Button Content="{Binding [Action.SignIn], Source={x:Static theming:Localization.Current}}"
                  Margin="0,0,8,0" Click="OnClaudeSignIn" />
          <Button Content="{Binding [Action.SignOut], Source={x:Static theming:Localization.Current}}"
                  Click="OnClaudeSignOut" />
        </StackPanel>
      </Grid>

      <CheckBox x:Name="CodexEnabled" Content="Codex" />
      <Grid Margin="27,7,0,0">
        <TextBlock x:Name="CodexSession" Foreground="{DynamicResource Brush.Muted}" FontSize="11.5" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <Button Content="{Binding [Action.SignIn], Source={x:Static theming:Localization.Current}}"
                  Margin="0,0,8,0" Click="OnCodexSignIn" />
          <Button Content="{Binding [Action.SignOut], Source={x:Static theming:Localization.Current}}"
                  Click="OnCodexSignOut" />
        </StackPanel>
      </Grid>
      </StackPanel>
      </Border>
    </StackPanel>

    <Border Grid.Row="1" Height="16" Background="Transparent" />

    <Border Grid.Row="2" Style="{StaticResource Group}">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <TextBlock Text="{Binding [Settings.RefreshInterval], Source={x:Static theming:Localization.Current}}"
                 Margin="0,0,16,8" />
      <TextBox x:Name="RefreshMinutes" Grid.Column="1" Margin="0,0,0,8" />

      <TextBlock Grid.Row="1"
                 Text="{Binding [Settings.WarningAt], Source={x:Static theming:Localization.Current}}"
                 Margin="0,0,16,8" />
      <TextBox x:Name="WarningPercent" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" />

      <TextBlock Grid.Row="2"
                 Text="{Binding [Settings.CriticalAt], Source={x:Static theming:Localization.Current}}"
                 Margin="0,0,16,8" />
      <TextBox x:Name="CriticalPercent" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" />

      <TextBlock Grid.Row="3"
                 Text="{Binding [Settings.Language], Source={x:Static theming:Localization.Current}}"
                 Margin="0,0,16,12" />
      <ComboBox x:Name="LanguageChoice" Grid.Row="3" Grid.Column="1" Width="160" Margin="0,0,0,12"
                SelectionChanged="OnLanguageChanged" />

      <CheckBox x:Name="AlwaysOnTop" Grid.Row="4" Grid.ColumnSpan="2" Margin="0,0,0,10"
                Content="{Binding [Settings.AlwaysOnTop], Source={x:Static theming:Localization.Current}}" />
      <CheckBox x:Name="RunAtStartup" Grid.Row="5" Grid.ColumnSpan="2"
                Content="{Binding [Settings.RunAtStartup], Source={x:Static theming:Localization.Current}}" />
    </Grid>
    </Border>

    <Border Grid.Row="3" Height="1" Background="{DynamicResource Brush.Hairline}" Margin="0,18" />

    <TextBlock Grid.Row="4"
               Text="{Binding [Settings.Author], Source={x:Static theming:Localization.Current}}"
               FontSize="11.5"
               Foreground="{DynamicResource Brush.Muted}" />

    <Grid Grid.Row="5" Margin="0,20,0,0">
      <TextBlock x:Name="ValidationMessage"
                 Foreground="{DynamicResource Brush.Text.Critical}"
                 FontSize="11.5"
                 TextWrapping="Wrap"
                 Margin="0,0,16,0"
                 VerticalAlignment="Center" />
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
        <Button Content="{Binding [Action.Cancel], Source={x:Static theming:Localization.Current}}"
                MinWidth="84" Margin="0,0,8,0" IsCancel="True" />
        <Button Content="{Binding [Action.Save], Source={x:Static theming:Localization.Current}}"
                Style="{StaticResource PrimaryButton}"
                MinWidth="84" IsDefault="True" Click="OnSave" />
      </StackPanel>
    </Grid>
  </Grid>
</Window>
